# Url-Check

## Overview
**Url-Check** is a command-line tool for finding and reporting dead links (e.g., broken URLs) in a file. Users can use this tool to help locate broken URLs in an HTML page, for example. **Url-Check** is written in Python.

